# rename this file to gdb.yml and put a : in front of "name" if
# you want to use yaml like in old versions of Catalyst
name gdb

# from gitalist
# $feature{'blame'}{'default'} = [1];
<feature>
	<blame>
		default = 1
	</blame>
</feature>